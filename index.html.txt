<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>陳泓諭 Hong-Yu Chen | 角力摔技專班</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root { --gold: #d4af37; --red: #ff4d4d; --bg: #05070a; --card: #111827; }
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: "PingFang TC", sans-serif; }
        body { background: var(--bg); color: white; line-height: 1.6; overflow-x: hidden; }

        /* 浮動導航 */
        .fixed-contact { position: fixed; right: 20px; bottom: 30px; z-index: 100; display: flex; flex-direction: column; gap: 15px; }
        .contact-btn { width: 60px; height: 60px; border-radius: 50%; display: flex; justify-content: center; align-items: center; color: white; text-decoration: none; font-size: 28px; box-shadow: 0 5px 20px rgba(0,0,0,0.5); transition: 0.3s; animation: pulse 2s infinite; }
        .ig-btn { background: linear-gradient(45deg, #f09433, #dc2743, #bc1888); }
        @keyframes pulse { 0% { transform: scale(1); } 50% { transform: scale(1.1); } 100% { transform: scale(1); } }

        /* 首屏 */
        .hero { height: 100vh; background: linear-gradient(rgba(0,0,0,0.6), var(--bg)), url('hero.jpg'); background-size: cover; background-position: center top; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 20px; }
        .hero h1 { font-size: clamp(3rem, 12vw, 6rem); font-weight: 900; color: white; letter-spacing: 2px; line-height: 1.1; }
        .hero p { color: var(--gold); font-size: 1.2rem; font-weight: bold; margin-top: 15px; letter-spacing: 4px; border-top: 1px solid var(--gold); border-bottom: 1px solid var(--gold); padding: 5px 20px; }

        /* 資歷卡片 */
        .section { padding: 80px 20px; max-width: 1000px; margin: 0 auto; }
        .title { font-size: 2.2rem; color: var(--gold); text-align: center; margin-bottom: 50px; position: relative; }
        .title::after { content: ''; display: block; width: 50px; height: 3px; background: var(--red); margin: 10px auto; }
        .stat-card { background: var(--card); border-left: 5px solid var(--red); padding: 30px; margin-bottom: 25px; border-radius: 0 20px 20px 0; transition: 0.3s; }
        .stat-card:hover { background: #1f2937; transform: translateX(10px); }
        .stat-card h3 { color: var(--gold); font-size: 1.5rem; margin-bottom: 5px; }

        /* 影片 */
        .video-box { background: #000; border-radius: 25px; overflow: hidden; box-shadow: 0 0 40px rgba(212,175,55,0.3); margin-top: 20px; }
        video { width: 100%; display: block; }

        /* 按鈕 */
        .cta { text-align: center; padding: 120px 20px; background: linear-gradient(to bottom, var(--bg), #1a1a1a); }
        .btn { background: var(--red); color: white; padding: 22px 60px; font-size: 1.6rem; font-weight: 800; border: none; border-radius: 100px; text-decoration: none; display: inline-block; transition: 0.3s; box-shadow: 0 10px 30px rgba(255,77,77,0.5); }
        .btn:hover { transform: translateY(-5px); filter: brightness(1.1); }

        /* 動畫 */
        .reveal { opacity: 0; transform: translateY(40px); transition: 1s ease-out; }
        .reveal.active { opacity: 1; transform: translateY(0); }
    </style>
</head>
<body>

    <div class="fixed-contact">
        <a href="https://www.instagram.com/chen.02.08/" class="contact-btn ig-btn"><i class="fab fa-instagram"></i></a>
    </div>

    <header class="hero">
        <h1 class="reveal">陳泓諭<br>角力專班</h1>
        <p class="reveal">全運會冠軍｜全大運五連霸金牌</p>
    </header>

    <section class="section">
        <h2 class="title">🥇 戰士榮耀</h2>
        <div class="stat-card reveal"><h3>2× 全運會角力冠軍</h3><p>2021, 2023 National Games Champion</p></div>
        <div class="stat-card reveal"><h3>5× 全大運金牌 (五連霸)</h3><p>2020 - 2026 Intercollegiate Champion</p></div>
        <div class="stat-card reveal"><h3>國家代表隊國手</h3><p>U15 - U20 亞洲錦標賽代表隊</p></div>
    </section>

    <section class="section">
        <h2 class="title">🎬 現場教學演示</h2>
        <div class="video-box reveal">
            <video controls muted autoplay loop poster="hero.jpg"><source src="video1.mp4" type="video/mp4"></video>
        </div>
    </section>

    <section class="cta">
        <h2 class="reveal" style="margin-bottom: 40px; font-size: 2.5rem;">想學真正的角力嗎？</h2>
        <a href="https://www.instagram.com/chen.02.08/" class="btn reveal">私訊 IG 預約體驗</a>
        <p style="margin-top: 30px; color: #555;">Follow Instagram: @chen.02.08</p>
    </section>

    <script>
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => { if (entry.isIntersecting) entry.target.classList.add('active'); });
        }, { threshold: 0.1 });
        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    </script>
</body>
</html>